model:
  family: lewm

target_encoder:
  kind: shared

masking:
  strategy: block_targets

projector:
  kind: heavy_bn

regularizer:
  name: sigreg
  weight: 0.09

augment:
  profile: pathology_light

runtime:
  experiment_name: lewm_default
